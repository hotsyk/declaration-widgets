============
Installation
============

At the command line::

    $ easy_install declaration-widgets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv declaration-widgets
    $ pip install declaration-widgets
